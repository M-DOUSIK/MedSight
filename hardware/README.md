# hardware/

The hardware **documents** live in `../docs/`, with everything else, so that
every relative link between documents resolves the same way:

| Document | What it is |
|---|---|
| [`../docs/HARDWARE_SPEC.md`](../docs/HARDWARE_SPEC.md) | **the whole hardware document** - annotated parts view, the eleven wires, the bill of materials, the specifications block, the power path and the bring-up order |
| [`../docs/MEDSIGHT_HANDBOOK.md`](../docs/MEDSIGHT_HANDBOOK.md) | Part 4 is the same material laid out for somebody holding a jumper wire |

This directory holds the **files**: `cad/` for the printed parts.
